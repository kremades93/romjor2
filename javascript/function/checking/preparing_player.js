function character_selection(x) {
    if(x.id === "character1") {
        document.getElementById("character_selection").innerHTML = "Asia; tradition and honor"
    }
    if(x.id === "character2") {
        document.getElementById("character_selection").innerHTML = "Africa; culture and magic"
    }
}